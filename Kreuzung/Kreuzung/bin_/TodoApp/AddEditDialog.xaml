<Window x:Class="TodoApp.AddEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Task" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <!-- Formularlayout med Grid: etiketter till vanster, inmatning till hoger -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Rad 0: Titel (obligatorisk) -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Title *:"
                   VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TitleTextBox"
                 Margin="0,0,0,10" Padding="3"/>

        <!-- Rad 1: Beskrivning -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:"
                   VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DescriptionTextBox"
                 Margin="0,0,0,10" Padding="3"/>

        <!-- Rad 2: Prioritet (ComboBox med tre alternativ) -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Priority:"
                   VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="PriorityComboBox"
                  Margin="0,0,0,10">
            <ComboBoxItem Content="Low"/>
            <ComboBoxItem Content="Medium" IsSelected="True"/>
            <ComboBoxItem Content="High"/>
        </ComboBox>

        <!-- Rad 3: Forfallodatum (DatePicker) -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Due Date:"
                   VerticalAlignment="Center" Margin="0,0,0,10"/>
        <DatePicker Grid.Row="3" Grid.Column="1" x:Name="DueDatePicker"
                    Margin="0,0,0,10"/>

        <!-- Rad 5: Knappar (Save och Cancel) -->
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Click="SaveButton_Click"
                    Width="80" Height="30" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            <Button Content="Cancel" Click="CancelButton_Click"
                    Width="80" Height="30"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
