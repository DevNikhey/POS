<Window x:Class="UE3_Rechner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UE3_Rechner"
        mc:Ignorable="d"
        Title="UE3 Rechner" Height="450" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Click="Button_Click">Run</Button>
        <Label  Grid.Row="0" Grid.Column="1">Tokens</Label>
        <Label  Grid.Row="0" Grid.Column="2">Ergebnis</Label>

        <TextBox Grid.Row="1" Grid.Column="0" Name="InputTextBox"
                 AcceptsReturn="True" FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"/>

        <ListBox Grid.Row="1" Grid.Column="1" Name="TokensListBox">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="90" Text="{Binding Type}" Margin="5"/>
                        <TextBlock Text="{Binding Value}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBox Grid.Row="1" Grid.Column="2" Name="OutputTextBox"
                 IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
