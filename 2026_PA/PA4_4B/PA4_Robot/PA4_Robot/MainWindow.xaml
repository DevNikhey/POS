<Window x:Class="PA4_Robot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:robot="clr-namespace:RobotLibrary;assembly=RobotLibrary"
        xmlns:local="clr-namespace:PA4_Robot"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="1" Name="InputTextBox" AcceptsReturn="True"></TextBox>
        <Button Grid.Row="0" Click="Button_Click">Run</Button>
        <Label Grid.Column="1">Tokens</Label>
        <ListBox Grid.Row="1" Grid.Column="1" Name="TokensListBox">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="100" Text="{Binding Type}" Margin="5"/>
                        <TextBlock Text="{Binding Value}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock Grid.Column="2">Roboter</TextBlock>
        <robot:RobotField Grid.Row="1" Grid.Column="2" Name="RobotFieldControl"/>
    </Grid>
</Window>
