<UserControl x:Class="LoginRegister.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White" CornerRadius="8" Padding="30" MaxWidth="400"
            HorizontalAlignment="Center" VerticalAlignment="Center"
            Effect="{StaticResource CardShadow}" Margin="20">
        <StackPanel>
            <TextBlock Text="Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Email" Margin="0,0,0,4"/>
            <TextBox x:Name="txtIdentifier" Margin="0,0,0,12" Padding="8,6" FontSize="14"/>

            <TextBlock Text="Password" Margin="0,0,0,4"/>
            <PasswordBox x:Name="txtPassword" Margin="0,0,0,8" Padding="8,6" FontSize="14"/>

            <TextBlock x:Name="txtError" Foreground="Red" Margin="0,0,0,12"
                       TextWrapping="Wrap" Visibility="Collapsed"/>

            <Button Content="Login" Click="Login_Click" Padding="12,8" FontSize="14"
                    Background="#4A90D9" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Don't have an account? " VerticalAlignment="Center"/>
                <Button Content="Register" Click="SwitchToRegister_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock TextDecorations="Underline">
                                <ContentPresenter/>
                            </TextBlock>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Foreground" Value="Navy"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
